import { TokenCollectionSpecs } from '../interfaces/TokenSpecs';
export default class TokenSpecs {
    static generateTokenCollectionSpecs(tokenJson: any, dnaInHex?: boolean, keepLayerValues?: boolean): TokenCollectionSpecs;
}
